; Upload limits for Apache + PHP-FPM/CGI.
; Applied from the document root (public/). Not used by mod_php (see .htaccess).
; Keep in sync with public/.htaccess and config/uploads.php.
upload_max_filesize = 16M
post_max_size = 32M
max_input_vars = 5000
